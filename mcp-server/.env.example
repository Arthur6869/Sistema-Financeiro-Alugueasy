# Supabase — use the SERVICE ROLE key (not the anon key)
# This key bypasses RLS and gives MCP agents full database access
SUPABASE_URL=https://rlkmljeatapayiroggrp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
ALUGUEASY_INTERNAL_API_KEY=your-internal-api-key-here

# Base URL for the AlugEasy Next.js application (optional, for webhooks/links)
ALUGUEASY_BASE_URL=http://localhost:3000

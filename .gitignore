# ── Next.js ──
.next/
out/
build/

# ── MCP Server build ──
mcp-server/dist/
mcp-server/node_modules/

# ── Node ──
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Ambiente ──
.env
.env.local
.env.*.local
.env.production

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini
*.pem

# ── Editor ──
.vscode/settings.json
.idea/
*.swp
*.swo

# ── TypeScript build ──
*.tsbuildinfo
dist/

# ── Temporários / debug ──
*.bak
*.old
*.backup
*.tmp
test-*.ts
temp-*.ts
debug-*.ts

# ── Planilhas / dados brutos ──
dados jan/
dados fev/
dados*/
*.xlsx
*.xls
data.json

# ── Obsidian ──
*.canvas
*.base
.obsidian/

# ── Histórico arquivado ──
.docs-historico/

# ── Supabase local ──
supabase/.branches/
supabase/.temp/

# ── Vercel ──
.vercel
vercel/.vercel
.env.production.local

# ── Coverage ──
/coverage

# ── Next.js gerado (commitado por convenção) ──
# next-env.d.ts é gerado mas deve ser versionado conforme docs do Next.js
